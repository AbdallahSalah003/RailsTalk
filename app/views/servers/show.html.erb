<% content_for :title, "Showing server" %>

<%= turbo_frame_tag :dashboard do %>
  <div class="flex">
    <%= render "sidebar" %>
    <div class="w-full h-screen bg-indigo-500">
      <h1 class="text-2xl text-gray-100 text-center pt-5"> <%= @server.name %> </h1>
    </div>
  </div>
<% end %>