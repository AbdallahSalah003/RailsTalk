<% content_for :title, "New channel" %>

<%= turbo_frame_tag :dashboard do %>
    <div class="max-w-2xl w-full mx-auto pt-15">
      <div class="max-w-2xl w-full bg-gray-600 p-10 rounded-xl">
        <h2 class="font-bold text-3xl">New Channel</h2>

        <%= render "form", server_channel: @server_channel %>

        <%= link_to "Cancel", @server, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
      </div>
    </div>
<% end %>